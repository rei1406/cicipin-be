FROM eclipse-temurin:25-jdk-alpine

WORKDIR /app

RUN apk add --no-cache maven

COPY pom.xml .
COPY src src

ARG EMAIL_SERVICE_CONTAINER_PORT
EXPOSE ${EMAIL_SERVICE_CONTAINER_PORT}

CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.addResources=true"]
